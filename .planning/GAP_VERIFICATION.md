# Gap Analysis Verification — 2026-05-08

## Claim 1: Missing Policies
Verdict: **PARTIAL — AttendancePolicy and AnnouncementPolicy PRESENT but NOT WIRED; TaskPolicy MISSING**

Evidence:
- `src/app/Policies/AttendancePolicy.php` — 50 lines — EXISTS
- `src/app/Policies/AnnouncementPolicy.php` — 76 lines — EXISTS
- `src/app/Policies/ReportCardPolicy.php` — 55 lines — EXISTS
- `src/app/Policies/TaskPolicy.php` — DOES NOT EXIST (claim is TRUE for this one)
- Also present (not mentioned in report): `GradePolicy` (62L), `MeetingPolicy` (75L), `OfflineAssignmentPolicy` (97L), `SchoolClassPolicy` (49L), `StudentPolicy` (68L), `TeacherPolicy` (49L), `TeachingAssignmentPolicy` (33L)
- `src/app/Providers/AppServiceProvider.php` registers: `StudentPolicy`, `GradePolicy`, `TeacherPolicy`, `SchoolClassPolicy`, `TeachingAssignmentPolicy` via `Gate::policy()`
- `AttendancePolicy`, `AnnouncementPolicy`, `ReportCardPolicy` are present but NOT registered in any provider — they exist as dead classes
- `src/app/Models/User.php` line 9,14: uses `Laravel\Sanctum\HasApiTokens` (Gate IS active)
- No `AuthServiceProvider` exists — sole provider is `AppServiceProvider`

## Claim 2: AcademicYear/Semester Web CRUD Missing
Verdict: **TRUE**

Evidence:
- `find src/app/Http/Controllers -iname '*academic*' -o -iname '*semester*'` — no results
- `src/routes/web.php` — no routes matching `academic-year`, `semester`, or `tahun-ajaran`
- `src/resources/js/Pages/AcademicYears/` — directory does not exist
- `src/resources/js/Pages/Semesters/` — directory does not exist
- No controller, no routes, no Inertia pages for either resource

## Claim 3: Student Assignment Submission POST Missing
Verdict: **TRUE**

Evidence:
- `src/routes/api.php` lines 65-66: only `GET /student/assignments` and `GET /student/assignments/{id}` — no POST/submit route
- `src/app/Http/Controllers/Api/V1/MobileAssignmentController.php`: only `index()` and `show()` methods — no `submit()` or `store()`
- `floz_mobile/lib/features/student/assignments/` — files present: `assignment_remote_datasource.dart`, `assignment_repository_impl.dart`, `assignment_repository.dart`, `assignment_detail_screen.dart`
- Grep for `submitAssignment`, `submit_assignment`, `/submit` in the entire assignments feature directory — NO matches
- Both backend API and Flutter data layer are missing the submission capability entirely

## Claim 4: Dead Tenant Code in storage_service.dart
Verdict: **TRULY DEAD**

Evidence:
- `floz_mobile/lib/core/storage/storage_service.dart` lines 31-46: `saveTenant()`, `getTenant()`, `getTenantSlug()`, `deleteTenant()` all confirmed present
- `_keyTenant = 'tenant_data'` constant also present (line 8)
- Grep across all of `floz_mobile/lib/` and `floz_mobile/test/` for any of the four method names — the ONLY matches are the method definitions themselves inside `storage_service.dart`
- No callers anywhere in the codebase — all four methods are dead code
- `clearAuth()` (lines 72-75) calls `deleteToken()` and `deleteUser()` but NOT `deleteTenant()` — so logout doesn't even clean the tenant key

## Claim 5: ziggy.js gitignore
Verdict: **TRUE — should be gitignored, currently is not**

Evidence:
- `src/resources/js/ziggy.js` exists: 11,155 bytes, 5 lines, dated 2026-04-18
- `git log -- src/resources/js/ziggy.js` — empty output (never committed to repo history)
- `src/.gitignore` and root `.gitignore` — neither contains any `ziggy` entry
- `src/composer.json` contains `"tightenco/ziggy": "^2.6"` — file is auto-generated by `php artisan ziggy:generate`
- File appears in `git status` as `?? src/resources/js/ziggy.js` (untracked)
- Should be added to `.gitignore`; it is a build artifact, not source

## Final Recommendation

| Claim | Verdict | Action |
|-------|---------|--------|
| 1. Missing Policies | PARTIAL | `TaskPolicy` is genuinely missing. `AttendancePolicy`, `AnnouncementPolicy`, `ReportCardPolicy` exist but are unregistered — wire them in `AppServiceProvider` or delete. |
| 2. AcademicYear/Semester CRUD | TRUE | Real gap — no controller, routes, or pages exist. Build if admin needs to manage school years. |
| 3. Assignment Submission POST | TRUE | Real gap — both backend route+controller and Flutter data layer are incomplete. High priority if students need to submit work. |
| 4. Dead tenant code in storage_service.dart | TRUE | Truly dead — safe to delete all four methods plus `_keyTenant` constant. Also fix `clearAuth()` if it should clean all state. |
| 5. ziggy.js not gitignored | TRUE | Add `resources/js/ziggy.js` to `src/.gitignore`. Low-risk quick fix. |

Claims 1 (partially), 2, 3, 4, and 5 are all real and actionable. The gap report slightly overstated Claim 1 (three of four policies exist as files, just unregistered). Everything else is confirmed accurate.
