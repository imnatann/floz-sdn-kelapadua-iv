# Code Quality Audit — 2026-05-08

## 🚦 Test Suite Status

- **PHP (Pest):** ✅ GREEN — 207 passed, 954 assertions, 7.17s, 8 parallel workers. Zero failures.
- **Flutter (analyze):** ✅ CLEAN — 2 issues only:
  - `warning` — unused import `integration_test/integration_test.dart` in `integration_test/helpers.dart:3`
  - `info` — deprecated `withOpacity()` in `lib/shared/widgets/floz_bottom_nav.dart:28` (use `.withValues()`)

---

## 🧹 Cleanup Items

### Stale Tenant References (mobile)
- `floz_mobile/lib/core/storage/storage_service.dart:8,30-47,70-71` — `_keyTenant`, `saveTenant()`, `getTenant()`, `getTenantSlug()`, `deleteTenant()`, `clearAuth()` (keeps tenant comment) are **dead code**: none of these methods are called anywhere else in the codebase. The whole `// Tenant` section (lines 30–47) can be deleted, and `clearAuth` comment updated.

### Stale Tenant Reference (backend comment only)
- `src/config/school.php:11` — docblock says "replace the dynamic Tenant model used by the original multi-tenant framework." Benign comment; rename to "Static metadata for this single-school deployment." 1-line fix.

### Untracked Generated File
- `src/resources/js/ziggy.js` — 11 KB generated file sitting untracked. Not in any `.gitignore`. Should be added to `src/.gitignore` (it is a build artifact generated by `php artisan ziggy:generate`).

### Uncommitted Plan Update
- `docs/superpowers/plans/2026-04-16-phase5-p4-attendance-input.md` — modified but not committed (git shows it as unstaged). Stale working-tree state; commit or stash before starting next phase.

### Flutter Deprecation
- `floz_mobile/lib/shared/widgets/floz_bottom_nav.dart:28` — `withOpacity()` deprecated since Flutter 3.x; replace with `.withValues(alpha: ...)`.

### Unused Import (Flutter test helper)
- `floz_mobile/integration_test/helpers.dart:3` — unused `package:integration_test/integration_test.dart` import; remove.

---

## 🔒 Security/Config

- **.env not committed:** ✅ `git ls-files src/.env` returns empty — no secrets tracked.
- **API rate limiting:** ✅ Present and scoped:
  - `throttle:login` on auth routes (`api.php:31`)
  - `throttle:mobile-api` on all authenticated mobile routes (`api.php:36`)
- **Sanctum config:** No `config/sanctum.php` found — Sanctum is configured via `config/auth.php` guards or published into a non-standard path. Verify `SANCTUM_STATEFUL_DOMAINS` is set correctly in `.env` for the production deployment.
- **Ziggy.js untracked:** Generated JS route file. If it reaches production via CI artifact, no immediate risk, but it should be gitignored to avoid accidental commit of environment-specific routes.

---

## 📝 TODO/FIXME Inventory

**Count: 0** — No `TODO`, `FIXME`, or `XXX` markers found in `src/app/**/*.php` or `floz_mobile/lib/**/*.dart`.

---

## 🎯 Recommended Pre-Development Cleanup (ranked)

1. **Commit or stash the attendance plan doc** — `docs/superpowers/plans/2026-04-16-phase5-p4-attendance-input.md` is modified but unstaged. This is a branch hygiene blocker; resolve before merging `chore/remove-tenant-leftovers` to `main`.

2. **Delete dead tenant code in StorageService** — `floz_mobile/lib/core/storage/storage_service.dart` lines 8, 30–47 (`_keyTenant`, `saveTenant`, `getTenant`, `getTenantSlug`, `deleteTenant`) are unreferenced. Also update the `clearAuth` comment. This is the last substantive tenant leftover in the mobile codebase.

3. **Gitignore `ziggy.js`** — Add `resources/js/ziggy.js` to `src/.gitignore`. It is a build artifact (generated by `php artisan ziggy:generate`) and should not be tracked or accidentally committed.

4. **Fix Flutter deprecation** — Replace `withOpacity()` with `.withValues(alpha: ...)` in `floz_mobile/lib/shared/widgets/floz_bottom_nav.dart:28`. Minor now; will become an error in a future Flutter SDK.

5. **Clean up `school.php` comment** — Update the docblock at `src/config/school.php:11` to remove the "replace the dynamic Tenant model" wording. Low priority (comment only, no runtime impact), but confusing for onboarding.

---

## Branch Status

- Current branch: `chore/remove-tenant-leftovers` (synced with remote)
- Unmerged local branches: `docs/database-class-diagram-and-cardinality-table` (also on remote, unreviewed)
- No uncommitted code changes — only the plan doc (docs) and untracked `ziggy.js`
